{
    "scripts": {
        "start": "cd frontend && npx electron/rebuild && cd .. node-gyp configure && node-gyp build && cd frontend && npm start"
    },
    "dependencies": {
        "nan": "^2.22.2",
        "node-addon-api": "^8.3.1"
    }
}
